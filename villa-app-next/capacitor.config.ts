import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.lavillastjean.app.next',
  appName: 'Villa Saint-Jean',
  webDir: 'dist',
  server: { androidScheme: 'https' },
}

export default config
